
/*
// Retrieves the index and newIdMediaShownInLightBox of previous media (to the left)
export function previous() {
  if (theIndex !== null) {
    // Based on theIndex
    if (theIndex === -1) {
      console.log('Error in previous() based on theIndex');
    } else if (theIndex === 0) {
      theIndex = mediasInLightBoxes.length - 1;
      newIdMediaShownInLightBox = mediasIdInLightBox[theIndex];
    } else {
      theIndex--;
      newIdMediaShownInLightBox = mediasIdInLightBox[theIndex];
    }
  } else {
    // Based on theIndexBis
    if (theIndexBis === -1) {
      console.log('Error in previous() based on theIndexBis');
    } else if (theIndexBis === 0) {
      theIndex = mediasInLightBoxes.length - 1;
      newIdMediaShownInLightBox = mediasIdInLightBox[theIndex];
    } else {
      theIndex = theIndexBis - 1;
      newIdMediaShownInLightBox = mediasIdInLightBox[theIndex];
    }
  }
}
*/

// export function previous(theIndex, theIndexBis, mediasInLightBoxes, newIdMediaShownInLightBox, mediasIdInLightBox) {

/*
// import { nextPrevDisplayMedia  } from '../pages/photographer-page.js';

// Below PreviousFactory model

class PreviousFactory {

  constructor(mediasInLightBoxes, mediasIdInLightBox, 
    newIdMediaShownInLightBox, theIndex, theIndexBis) {
    this._mediasInLightBoxes = mediasInLightBoxes;
    this._mediasIdInLightBox = mediasIdInLightBox;
    this._newIdMediaShownInLightBox = newIdMediaShownInLightBox;
    this._theIndex = theIndex;
    this._theIndexBis = theIndexBis;
  }
  previousMod() {
    */
/*
    if (this._theIndex !== null) {
      // Based on theIndex
      if (this._theIndex === -1) {
        console.log('Error in previous()');
      } else if (this._theIndex === 0) {
        this._theIndex = this._mediasInLightBoxes.length - 1;
        this._newIdMediaShownInLightBox = this._mediasIdInLightBox[this._theIndex];
        const Rbn = 3;
        // const Rbt = 6;
        nextPrevDisplayMedia();
        // const modifiedTwoValues = [ Rbn, Rbt ];
        return Rbn;
      } else {
        this._theIndex--;
        this._newIdMediaShownInLightBox = this._mediasIdInLightBox[this._theIndex];
        const Rbn = 3;
        // const Rbt = 6;
        nextPrevDisplayMedia();
        // const modifiedTwoValues = [ Rbn, Rbt ];
        return Rbn;
      }
    } else {
      // Based on theIndexBis
      if (this._theIndexBis === -1) {
        console.log('Error in previous()');
      } else if (this._theIndexBis === 0) {
        this._theIndex = this._mediasInLightBoxes.length - 1;
        this._newIdMediaShownInLightBox = this._mediasIdInLightBox[this._theIndex];
        const Rbn = 3;
        // const Rbt = 6;
        nextPrevDisplayMedia();
        // const modifiedTwoValues = [ Rbn, Rbt ];
        return Rbn;
      } else {
        this._theIndex = this._theIndexBis - 1;
        this._newIdMediaShownInLightBox = this._mediasIdInLightBox[this._theIndex];
        const Rbn = 3;
        // const Rbt = 6;
        nextPrevDisplayMedia();
        // const modifiedTwoValues = [ Rbn, Rbt ];
        return Rbn;
      }
      */
/*
      Rbn = 3;
      return Rbn;
    }
}
*/
